{
  "_comment": "Merge these into the target project's package.json. Versions are recent stable as of 2026-05; bump if needed.",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "otplib": "^12.0.1"
  },
  "scripts": {
    "test:fixture-init": "dotenv -e .env.local -- tsx e2e/fixture-init.ts",
    "e2e": "dotenv -e .env.local -- playwright test --config e2e/playwright.config.ts",
    "e2e:ui": "dotenv -e .env.local -- playwright test --config e2e/playwright.config.ts --ui"
  }
}
